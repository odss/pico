import { blockFactory } from '../base';


export default [
    blockFactory('core.section', 'core.layout'),
    blockFactory('core.container', 'core.layout'),
    blockFactory('core.column', 'core.layout')
];