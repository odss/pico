import { Block, blockFactory } from '../base';


export default [
    blockFactory('core.list', 'core.basic'),
    blockFactory('core.link', 'core.basic'),
    blockFactory('core.heading', 'core.basic'),
    blockFactory('core.text', 'core.basic'),
]
